#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H
/**
 * ABS - Function like macro that computes the
 * absolute value of a number x
 * @x: Variable to be computer by ABS
 */
#define ABS(x) ((x) < 0 ? -(x) : (x))
#endif
