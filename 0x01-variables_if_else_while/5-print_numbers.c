#include <stdio.h>
/**
* main - Entry point
* x - stores the value of the number being parsed
* Return: Always 0 (Sucess)
*/
int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
		printf("%d", x);
	}
	printf("\n");
	return (0);
}
